# مستندات نهایی پروژه V2Ray Config Manager (Full Document)

## ۱. معرفی و اهداف پروژه
پروژه **V2Ray Config Manager** یک اپلیکیشن اندرویدی حرفه‌ای مبتنی بر React Native (Expo) است که برای مدیریت، تست و اتصال به کانفیگ‌های VPN طراحی شده است. این اپلیکیشن با حذف لایه‌های واسط، مستقیماً با Cloudflare Workers در ارتباط است و تمامی پردازش‌های حساس از جمله تست و اتصال را در سمت کلاینت انجام می‌دهد.

### اهداف محقق شده:
- **معماری Serverless:** حذف کامل بک‌اِند FastAPI و دیتابیس MongoDB و جایگزینی با ارتباط مستقیم Worker.
- **اتصال واقعی (Real Tunneling):** طراحی و آماده‌سازی زیرساخت Native Bridge برای برقراری اتصال واقعی VPN.
- **تست حرفه‌ای (Real Delay):** پیاده‌سازی متد تست تاخیر واقعی در لایه اپلیکیشن.
- **مدیریت اشتراک پیشرفته:** سیستم هوشمند دریافت آمار مصرف و زمان باقی‌مانده از طریق کد اشتراک یا QR Code.
- **رابط کاربری مدرن:** طراحی الهام گرفته از WARP و Windscribe با تم تاریک و انیمیشن‌های روان.

---

## ۲. معماری نهایی سیستم

### کلاینت (Android App):
- **Core:** React Native 0.81.5 + Expo SDK 54.
- **State Management:** Zustand (برای پایداری و سرعت بالای دسترسی به داده‌ها).
- **Communication:** apiService (ارتباط مستقیم با Cloudflare Worker).
- **Testing:** testService (اجرای تست‌های غیرمسدودکننده در لایه کلاینت).
- **Parsing:** parser.ts (پشتیبانی کامل از VLESS, VMess, Trojan, Shadowsocks).

### بک‌اِند (Cloudflare Worker):
- **Storage:** KV Storage برای نگهداری هزاران کانفیگ رتبه‌بندی شده.
- **Endpoints:**
  - `/api/configs`: دریافت کانفیگ‌های عمومی.
  - `/api/user-sub`: دریافت دیتای اختصاصی کاربر (حجم، زمان، کانفیگ).
  - `/dashboard/api/vote`: سیستم ثبت امتیاز جهت بهبود کیفیت.

---

## ۳. ویژگی‌های فنی پیاده‌سازی شده

### ۳.۱ مدیریت اشتراک (Subscription Tab)
کاربران می‌توانند با وارد کردن کد `AdminID-ClientID` اطلاعات زیر را مشاهده کنند:
- نمودار مصرف حجم (Used vs Total).
- تعداد روزهای باقی‌مانده از اعتبار.
- تعداد کاربران متصل به همان اشتراک.
- اسکنر QR داخلی برای سهولت در افزودن اشتراک.

### ۳.۲ صفحه اتصال هوشمند (Smart Connect)
- نمایش وضعیت ایمنی اتصال مشابه Cloudflare WARP.
- دکمه Power بزرگ با Glow Effect متحرک.
- انتخاب خودکار بهترین سرور بر اساس کمترین پینگ واقعی کلاینت.

---

## ۴. راهنمای بیلد و CI/CD (GitHub Actions)
این پروژه مجهز به سیستم بیلد خودکار است:
- **فایل تنظیمات:** `.github/workflows/android-release.yml`
- **نحوه عملکرد:** با هر بار Push کردن تگ جدید (مثلاً `v1.0.1`)، گیت‌هاب به صورت خودکار نسخه‌های اندروید را در تمامی معماری‌ها تولید کرده و در بخش **Releases** قرار می‌دهد.

---

## ۵. جزئیات پیاده‌سازی کد

### ۵.۱ منطق تست و پارس (سمت کلاینت)
تمامی پردازش‌ها به اپلیکیشن منتقل شد:
- `parser.ts`: پشتیبانی کامل از پارامترهای پیشرفته (TLS, Reality, gRPC, SNI, Path) جهت سازگاری با تمامی نسخه‌های V2Ray.
- `testService.ts`: اجرای تست‌های Real Delay با قابلیت انتخاب هدف تست دلخواه.

### ۵.۲ سرویس اتصال (V2RayService)
رابط کاربری Bridge برای اتصال واقعی:
- `V2RayService.ts`: کنترل اتصال با پشتیبانی از حالت توسعه (`__DEV__`) جهت شبیه‌سازی هوشمند.
- **Native Modules:** کدهای کاتلین در پوشه `native_source` شامل `V2RayVpnService` جهت برقراری تونل VPN در اندروید.

### ۵.۳ انتخاب هدف تست (Custom Test Target)
برنامه به کاربران اجازه می‌دهد سرور یا وب‌سایت هدف برای تست پینگ را انتخاب کنند. لیست این اهداف از ورکر دریافت شده و شامل مشخصات کامل (نام، آدرس، پورت و متد) می‌باشد.

### ۵.۴ مدیریت هوشمند حافظه لوکال و آفلاین
برنامه مجهز به سیستم ذخیره‌سازی پیشرفته جهت افزایش سرعت و پایداری است:
- **کش ۲۴ ساعته:** کانفیگ‌های دریافت شده تا ۲۴ ساعت معتبر تلقی شده و در صورت لزوم از حافظه لوکال لود می‌شوند.
- **اولویت‌بندی تست:** در هنگام اتصال، ابتدا کانفیگ‌های لایک شده و سپس کانفیگ‌های با سابقه موفقیت تست می‌شوند.
- **پاکسازی خودکار:** کانفیگ‌هایی که بیش از ۳ روز سابقه تست مثبت نداشته باشند، جهت بهینه‌سازی حافظه حذف می‌شوند.
- **عملکرد آفلاین:** در صورت قطع اینترنت، برنامه از آخرین لیست معتبر خود برای تست و اتصال استفاده می‌کند.

---

## ۶. ساختار پوشه‌بندی نهایی
```
frontend/
├── app/
│   ├── (tabs)/              # صفحات اصلی برنامه
│   ├── constants/           # ثابت‌های رنگی و API
│   ├── store/               # مدیریت حالت (Zustand)
│   ├── services/            # سرویس‌های API، تست و VPN
│   ├── utils/               # توابع کمکی و پارسر
│   └── types/               # تعاریف TypeScript
native_source/               # سورس‌کدهای Native اندروید (Kotlin)
.github/workflows/           # تنظیمات بیلد خودکار
```

---
*پایان مستندات - تمامی حقوق برای V2Ray Manager محفوظ است.*
